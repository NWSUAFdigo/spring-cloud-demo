server:
  port: 8082

spring:
  application:
    name: client-feign

#eureka:
#  client:
#    fetch-registry: true
#    register-with-eureka: true
#    service-url:
#      defaultZone: http://localhost:8090/eureka/

# 启动断路器
feign:
  hystrix:
    enabled: true
